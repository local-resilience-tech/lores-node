{
  "name": "lores-node",
  "private": true,
  "version": "0.10.1",
  "devDependencies": {
    "@j-ulrich/release-it-regex-bumper": "^5.0.0",
    "release-it": "^18.1.0"
  },
  "scripts": {
    "release": "release-it",
    "swarm": "sudo docker build -f ./backend/Dockerfile-development -t lores-node-dev ./backend; sudo docker stack deploy --detach=false --prune -c ./docker-swarm-local-dev.yml lores-node-dev"
  }
}
